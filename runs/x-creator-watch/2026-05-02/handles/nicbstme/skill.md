# Skill Distillation: @nicbstme

## Purpose

Use `@nicbstme` as a source for agent memory engineering, Microsoft knowledge-worker AI, Fintool acquisition signals, memory portability, memory-security boundaries, and Codex browser execution examples.

## Workflow

1. Capture profile, pinned acquisition article, Agent Memory Engineering posts, Codex/Claude comparisons, and browser-task examples.
2. Classify posts by knowledge-worker AI, finance workflow, acquisition, memory generation, memory portability, memory security, prompt injection, setup migration, or browser execution.
3. Extract implementation mechanics: real-time write, async idle write, background job, memory store path, system-prompt insertion, security scanner, and setup import.
4. Map each memory claim into Jarvis controls: provenance, trust level, sanitization, review queue, write cadence, and rollback.
5. Treat real-world browser task examples as UX signals and preserve confirmation requirements for purchases.

## Output Template

```markdown
## Nicolas Agent Memory Note

- Source:
- Memory mechanism:
- Security concern:
- Product surface:
- Related agent:
- Jarvis control:
```

## Failure Modes

- Treating all remembered content as equally trusted.
- Missing the timing difference between real-time and async memory writes.
- Copying adversarial examples into operational prompts without sanitization.
