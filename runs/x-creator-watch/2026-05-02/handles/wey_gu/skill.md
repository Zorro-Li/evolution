# Skill Distillation: @wey_gu

## Purpose

Use `@wey_gu` as a source for local-first agent memory, Nowledge Mem, cross-agent context management, con terminal harness, terminal state restoration, and feedback-driven AI tooling.

## Workflow

1. Capture profile, pinned Nowledge Mem posts, con release posts, GitHub links, terminal-state features, and user-feedback quotes.
2. Classify posts as memory architecture, local-first compute, distillation, terminal harness, state restore, layout system, release note, or community feedback.
3. Extract state primitives: terminal history, current path, layout, dot-file layout, directory layout, context retrieval, sinking, and distillation.
4. Map each primitive to Jarvis: session restore, project operating state, cross-agent memory, reflection, and durable rule promotion.
5. Track platform readiness across macOS, Windows, and Linux Desktop.

## Output Template

```markdown
## Wey Gu / Local Agent Memory Note

- Source:
- Product:
- Memory or terminal primitive:
- Local-first claim:
- State artifact:
- Jarvis reuse:
```

## Failure Modes

- Reading con as a generic terminal instead of an AI harness.
- Capturing memory claims without local-first persistence and compute details.
- Missing state-restore primitives inside short Chinese release notes.
