# Skill Distillation: @dani_avila7

## Purpose

Use `@dani_avila7` as a source for Claude Code subagents, skills, hooks, context hygiene, OSS AI templates, and Anthropic developer-platform signals.

## Workflow

1. Capture profile, pinned article, Claude Code subagent posts, skills/hooks posts, OSS template links, and high-bookmark context-management posts.
2. Classify posts as subagent architecture, context hygiene, skill/hook template, OSS scaffold, Anthropic platform demand, token-efficiency note, or builder-shipping update.
3. Extract the decomposition rule: which work goes to a subagent, what context stays isolated, and what result returns to the main session.
4. Convert subagent patterns into Jarvis worker delegation rules and output contracts.
5. Watch `aitmpl.com` for reusable templates that can be installed or ported.

## Output Template

```markdown
## Daniel San / Claude Code Subagent Note

- Source:
- Problem:
- Subagent boundary:
- Returned artifact:
- Template or hook:
- Jarvis reuse:
```

## Failure Modes

- Capturing the subagent term without the context-hygiene mechanism.
- Mixing high-signal pinned article evidence with casual low-metric jokes.
- Missing OSS template links when posts mention `aitmpl.com`.
